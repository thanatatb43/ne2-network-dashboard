# มาตรฐาน UX/UI — NE2 Network Dashboard

เอกสารอ้างอิงสำหรับการเพิ่มหน้าใหม่และปรับปรุงหน้าปัจจุบัน เพื่อให้ผู้ใช้เรียนรู้รูปแบบครั้งเดียวแล้วใช้งานต่อได้ทั้งระบบ

## 1. ขอบเขตและวิธีใช้

- ใช้มาตรฐานนี้กับงาน UI ใหม่ และค่อย ๆ ปรับหน้าที่กำลังแก้ไข ไม่จำเป็นต้องเปลี่ยนทุกหน้าพร้อมกัน
- ใช้ [Devices.jsx](src/components/Devices.jsx) และ [EquipmentSearch.jsx](src/components/EquipmentSearch.jsx) เป็นตัวอย่างหน้ารายการ ทั้งสองหน้าใช้ [ListPage.css](src/components/ListPage.css) ร่วมกันผ่านคลาส `list-*` ส่วนสีธีมเดิมอยู่ใน [index.css](src/index.css)
- หน้า Devices เป็นงานนำร่อง ไม่ได้หมายความว่าทุกหน้าใช้มาตรฐานนี้แล้ว หรือผ่านการตรวจ accessibility และทุก viewport แล้ว
- ฟอร์ม modal และคอมโพเนนต์กลางที่ระบุในเอกสารเป็นแนวทางสำหรับงานถัดไป ยังไม่ได้มี implementation กลางครบทุกตัว
- รักษา business rules สิทธิ์ผู้ใช้ ขอบเขตการส่งออก และพฤติกรรมการกลับหน้าเดิม หากจำเป็นต้องเปลี่ยน ให้ระบุเหตุผลและผลต่อผู้ใช้ในรายละเอียดงาน
- หากลักษณะงานต้องใช้รูปแบบต่างออกไป เช่น แผนที่ กราฟ หรือฟอร์มยาว ให้ปรับ layout ตามงาน แต่คงภาษา สีสถานะ controls และ feedback ตามมาตรฐาน

## 2. หลักการออกแบบ

1. ให้ผู้ใช้เห็นว่ากำลังอยู่หน้าไหน ทำอะไรได้ และข้อมูลล่าสุดเมื่อใด
2. ใช้ภาษาไทยสำหรับคำสั่งและคำอธิบาย เก็บศัพท์เทคนิคที่จำเป็น เช่น IP, Latency, Serial Number และ Excel
3. ใช้ม่วงเป็นสีการกระทำหลัก เหลืองเป็นสีรองของแบรนด์ พื้นที่ข้อมูลเน้นความอ่านง่าย
4. ความหมายของสี ตำแหน่งคำสั่ง และพฤติกรรม controls ต้องสม่ำเสมอ
5. แยกข้อมูลว่าง ข้อมูลผิดพลาด และข้อมูลเก่าอย่างชัดเจน ห้ามตีความว่าโหลดไม่สำเร็จเท่ากับระบบปกติ
6. รักษาบริบทของงานเมื่อเปิดรายละเอียดแล้วกลับรายการ ผู้ใช้ไม่ควรต้องค้นหาหรือกรองใหม่โดยไม่จำเป็น

## 3. สีและธีม

ใช้ CSS variables ตามบทบาท หลีกเลี่ยงสีข้อความและพื้นหลังตายตัวใน JSX โดยเฉพาะสีดำบนพื้นผิวที่เปลี่ยนเป็น dark theme

| บทบาท | ตัวแปรที่มีอยู่ | การใช้งาน |
|---|---|---|
| พื้นหลังหน้า | `--bg-color` | พื้นที่หลัก |
| พื้นผิวข้อมูล | `--card-bg` | panel และ card |
| ข้อความหลัก/รอง | `--text-primary`, `--text-secondary` | เนื้อหาและคำอธิบาย |
| ช่องกรอก | `--input-bg`, `--input-border` | input, select, textarea |
| เส้นแบ่ง | `--border-subtle` | ตารางและ panel |
| สีแบรนด์ | `--accent-primary`, `--accent-secondary` | ม่วงและเหลือง |
| พื้นสถานะ | `--bg-success-subtle`, `--bg-danger-subtle`, `--bg-accent-subtle` | badge, feedback, hover |

หน้า Devices ใช้สีข้อความเฉพาะบทบาทเพิ่ม เพื่อให้อ่านง่ายกว่านำสีแบรนด์มาใช้กับข้อความเล็กโดยตรง:

| บทบาท | Light | Dark |
|---|---|---|
| ลิงก์และ focus | `#7e22ce` | `#d8b4fe` |
| สถานะสำเร็จ/ออนไลน์ | `#15803d` | `#86efac` |
| สถานะผิดพลาด/ขัดข้อง | `#b91c1c` | `#fca5a5` |
| ปุ่มหลักพื้นทึบ | `#7e22ce` และข้อความขาว | เหมือน light |
| ปุ่มหลักเมื่อ hover | `#6b21a8` และข้อความขาว | เหมือน light |

ค่าข้างต้นใช้ผ่าน semantic tokens `--list-action`, `--list-success`, `--list-danger` ใน `.list-page` ของ `ListPage.css` สำหรับหน้ารายการร่วมกัน ตรวจความอ่านง่ายกับพื้นหลังจริงทั้งสองธีมก่อนส่งงาน โดยเฉพาะพื้นโปร่งใส สีเตือน และกราฟ

- เขียว = สำเร็จ/ออนไลน์, แดง = ผิดพลาด/ขัดข้อง/ลบ, สีรองเป็นกลาง = ไม่ทราบสถานะ
- ใช้ข้อความหรือไอคอนประกอบสีเสมอ เช่น “ออนไลน์”, “ขัดข้อง”, “ไม่ทราบสถานะ”
- ห้ามแสดงค่าที่ไม่มีเป็น `0` ถ้า 0 มีความหมายจริง ใช้ “—” หรือ “ไม่มีข้อมูล”

## 4. ตัวอักษร ระยะห่าง และรูปทรง

| รายการ | ค่าเริ่มต้น |
|---|---|
| ฟอนต์หลัก | Krub ตาม `index.css`; controls ใช้ `font: inherit` |
| หัวข้อหน้า | 24–32px, น้ำหนัก 700, line-height ประมาณ 1.4 |
| หัวข้อส่วน | 18–20px, น้ำหนัก 600–700 |
| เนื้อหาและฟอร์ม | 14–16px, line-height อย่างน้อย 1.5 |
| คำอธิบายรองและ badge | 13–14px; ไม่ใช้กับข้อมูลสำคัญที่ต้องอ่านต่อเนื่อง |
| หน่วยระยะห่าง | 4, 8, 12, 16, 20, 24, 32, 48px |
| ระหว่างหัวหน้ากับเนื้อหา | 24px |
| padding ของ panel | 20px; บนจอเล็ก 16px |
| ช่องว่าง label กับ control | 8px |
| มุมโค้ง control | 8px |
| มุมโค้งข้อความแจ้งเตือน | 12px |
| มุมโค้ง panel | 16px |
| มุมโค้ง badge | 999px |
| ความสูงปุ่มและช่องกรอกทั่วไป | อย่างน้อย 44px |

ใช้ monospace เฉพาะข้อมูลอย่าง IP และใช้ `font-variant-numeric: tabular-nums` กับตัวเลขที่ต้องเปรียบเทียบ ไม่ใช้ gradient กับข้อความข้อมูลสำคัญ หลีกเลี่ยง `.glass` ที่มีมุมโค้งเดิม 48px สำหรับ panel งานใหม่โดยไม่ปรับให้เหมาะสม

## 5. โครงสร้างหน้ารายการ

จัดลำดับเนื้อหาจากบนลงล่างดังนี้:

1. **Page header:** ชื่อหน้า `h1` หนึ่งรายการ คำอธิบายสั้น และคำสั่งระดับหน้า
2. **ข้อความแจ้งปัญหา:** แสดงเมื่อโหลดผิดพลาดหรือข้อมูลเก่า พร้อมทางแก้
3. **Toolbar:** ช่องค้นหา ตัวกรองหลัก และ “ล้างตัวกรอง”
4. **สรุปผล:** จำนวนที่พบ/จำนวนทั้งหมด เวลาอัปเดต และลำดับเริ่มต้นถ้ามี
5. **ตารางหรือรายการ:** ข้อมูลและลิงก์ไปยังรายละเอียด
6. **Footer:** จำนวนรายการต่อหน้า ช่วงรายการที่แสดง และ pagination

ใช้ primary action ไม่เกินหนึ่งรายการต่อกลุ่มงาน ให้การรีเฟรชและคำสั่งรองใช้ปุ่มรอง หากหน้าเน้นการอ่านข้อมูลและไม่มีคำสั่งหลัก ไม่ต้องสร้างปุ่มหลักเพิ่มเพียงเพื่อเติม layout

## 6. ปุ่ม ลิงก์ และไอคอน

- **Primary:** พื้นม่วงทึบ สำหรับงานหลัก เช่น “บันทึก”, “เพิ่มอุปกรณ์”
- **Secondary:** พื้นตามธีมและเส้นขอบ สำหรับ “ยกเลิก”, “รีเฟรช”, “ล้างตัวกรอง”
- **Danger:** ใช้แดงเฉพาะการกระทำที่ทำลายหรือยกเลิกข้อมูล แยกจากปุ่มหลัก และยืนยันเมื่อผลย้อนคืนยาก
- ใช้ `<button>` สำหรับการกระทำ และ `<a href>` สำหรับนำทาง ให้ลิงก์เปิดแท็บใหม่ได้ตามปกติ
- ใช้ไอคอนจาก `lucide-react` ขนาด 16–20px ใน controls พร้อมข้อความเมื่อความหมายอาจไม่ชัด
- ปุ่มไอคอนล้วนต้องมี accessible name เช่น `aria-label`; tooltip อย่างเดียวไม่เพียงพอ
- ระหว่างบันทึก/ส่งคำขอ ป้องกันการกดซ้ำและแสดงข้อความ เช่น “กำลังบันทึก…”
- ใน form ระบุ `type="button"` ให้ปุ่มที่ไม่ใช่ submit
- อย่าใช้ hover ยกการ์ดกับ panel ที่กดไม่ได้

## 7. ค้นหา ตัวกรอง และการจำบริบท

เลือกวิธีเก็บค่าจากพฤติกรรมที่ต้องการ ไม่บังคับให้ทุกหน้าใช้ storage แบบเดียวกัน

| วิธี | เหมาะกับ |
|---|---|
| React state | ค่าชั่วคราวที่ไม่จำเป็นต้องคงอยู่หลัง unmount |
| ยก state ไป parent | การเปลี่ยน view ภายใน flow เดียวกันที่ parent ยังอยู่ |
| sessionStorage | จำบริบทในแท็บเดิม รวมถึงเมื่อรีเฟรชหน้า |
| URL query | ตัวกรองที่ต้องแชร์ผ่านลิงก์ หรือย้อนตามประวัติการค้นหาด้วย Back/Forward |

- หน้ารายการควรคืนคำค้น ตัวกรอง sort จำนวนรายการต่อหน้า และหน้าที่เลือกเมื่อกลับจากรายละเอียด
- การจำตัวกรองไม่เท่ากับการ cache ผลลัพธ์: สามารถใช้เงื่อนไขเดิมโหลดข้อมูลล่าสุดได้
- อย่าล้างตัวกรองเมื่อ mount หรือเปลี่ยน route โดยไม่มีเหตุผลจาก flow
- เปลี่ยนคำค้น/ตัวกรอง/จำนวนต่อหน้าให้เริ่มหน้า 1; เปลี่ยน sort ให้ใช้พฤติกรรมเดียวกันในหน้าประเภทเดียวกัน
- หากข้อมูลลดลง ให้แสดงหน้าที่อยู่ในช่วงจริง อย่า reset หน้าที่จำไว้เพียงเพราะยังโหลดข้อมูลไม่เสร็จ
- namespace และ version storage key เช่น `network-devices:view:v1`; ตรวจรูปแบบค่าที่อ่านและรองรับ storage ใช้ไม่ได้โดยหน้ายังทำงาน
- ห้ามเก็บ token หรือข้อมูลละเอียดอ่อนเพิ่มลง storage สำหรับการจำมุมมอง และแยก/ล้างค่าที่ขึ้นกับบัญชีเมื่อเปลี่ยนผู้ใช้ตามความเหมาะสม
- ค้นหาในข้อมูลที่โหลดแล้วทำทันทีได้; ค้นหาผ่าน API เลือก debounce หรือปุ่ม “ค้นหา” ตามต้นทุนและ flow เดิม พร้อมป้องกัน response เก่าทับคำค้นใหม่
- “ล้างตัวกรอง” ล้างเฉพาะเงื่อนไขการค้นหา ไม่เปลี่ยน sort หรือจำนวนต่อหน้าโดยไม่แจ้ง
- การคืน scroll/focus ให้พิจารณาตาม flow และทดสอบจริง หน้า Devices ปัจจุบันยังไม่ได้เพิ่มกลไกคืน scroll/focus โดยเฉพาะ

## 8. ตารางและการส่งออก

- ใช้ HTML table สำหรับข้อมูลที่มีความสัมพันธ์แบบแถว/คอลัมน์ พร้อมชื่อที่เข้าถึงได้และ `scope="col"`
- หัวตารางที่เรียงได้ต้องมีปุ่มและ `aria-sort`; แสดงทิศทางการเรียงอย่างชัดเจน
- ชื่อรายการเป็นลิงก์ไปหน้ารายละเอียด แม้รองรับคลิกทั้งแถวก็ต้องมีลิงก์ที่ใช้คีย์บอร์ดได้
- ตารางรายการใช้ข้อความหนึ่งบรรทัดต่อช่อง เพื่อให้ความสูงแถวสม่ำเสมอ ข้อความเกินพื้นที่แสดง `…` ด้วย CSS (`white-space: nowrap`, `overflow: hidden`, `text-overflow: ellipsis`) โดยเก็บข้อความเต็มไว้ใน DOM ไม่ตัดข้อมูลจริง
- กำหนดความกว้างคอลัมน์ให้มีขอบเขต เช่น `table-layout: fixed` และสัดส่วนคอลัมน์ ชื่อรายการควรได้พื้นที่มากกว่าประเภทหรือสถานะ ส่วน flex item ที่ตัดข้อความต้องมี `min-width: 0`
- ข้อความที่ตัดต้องมี `title` เป็นข้อความเต็มสำหรับ mouse hover และมีทางอ่านเต็มบนมือถือ/คีย์บอร์ดผ่านหน้ารายละเอียด ไม่ใช้ tooltip เป็นทางเดียวในการเข้าถึงข้อมูล
- ชื่อรายการใช้สีลิงก์โดยไม่ขีดใต้ คง hover และ focus ที่มองเห็นได้ ข้อมูลรองในช่องเดียวกัน เช่น รหัสทรัพย์สิน/Serial หรือผู้ถือครอง/รหัสพนักงาน แสดงต่อกันด้วยตัวคั่น; หาก flow ต้องเปรียบเทียบข้อความยาวหลายบรรทัด ให้ใช้รูปแบบรายละเอียดเฉพาะแทนตารางรายการ
- แสดงหน่วยในหัวคอลัมน์ เช่น `Latency (ms)` และ `Packet loss (%)` ใช้ทศนิยมสม่ำเสมอ
- แสดงสถานะด้วยข้อความร่วมกับไอคอนหรือสี ไม่ใช้สีเพียงอย่างเดียว
- เมื่อไม่มีสิทธิ์ดูค่า ให้แสดงคำอธิบายแทนค่าที่อ่านไม่ได้ เช่น “เข้าสู่ระบบเพื่อดู IP”; การซ่อน UI ไม่ทดแทนการตรวจสิทธิ์ของ API
- มี “ก่อนหน้า”, “ถัดไป”, เลขหน้าปัจจุบัน และจำนวนผลลัพธ์ ปิดปุ่มเมื่อไปต่อไม่ได้
- ใช้จำนวนต่อหน้า 10, 25, 50, 100 เป็นค่าเริ่มต้น เว้นแต่ API หรือปริมาณข้อมูลกำหนดต่างออกไป
- ระบุขอบเขตการส่งออกบนปุ่ม เช่น “ส่งออกทั้งหมด (Excel)” หรือ “ส่งออกผลการค้นหา” และให้ตรงกับข้อมูลที่ส่งจริง
- รักษากติกาสิทธิ์ในไฟล์ส่งออกเหมือนการดูข้อมูลบนหน้า

## 9. สถานะข้อมูลและ feedback

| สถานการณ์ | สิ่งที่ต้องแสดง |
|---|---|
| โหลดครั้งแรก | loading/skeleton พร้อมข้อความที่บอกว่ากำลังโหลดอะไร |
| โหลดสำเร็จและมีข้อมูล | รายการ จำนวนผลลัพธ์ และเวลาอัปเดตถ้าข้อมูลเปลี่ยนตามเวลา |
| โหลดสำเร็จแต่ไม่มีข้อมูล | “ยังไม่มี…” พร้อมทางไปต่อที่เหมาะสม |
| ตัวกรองไม่พบผลลัพธ์ | “ไม่พบ…ที่ตรงกับตัวกรอง” และ “ล้างตัวกรอง” |
| โหลดครั้งแรกล้มเหลว | ข้อความผิดพลาดและ “ลองใหม่”; ไม่แสดงว่าไม่มีข้อมูลในระบบ |
| รีเฟรชล้มเหลวแต่มีข้อมูลเดิม | คงรายการเดิม พร้อมแจ้งว่าเป็นข้อมูลจากครั้งก่อน |
| กำลังรีเฟรช | คงข้อมูลเดิมและแสดงความคืบหน้าโดยไม่ทำให้รายการกระพริบ |
| บันทึกล้มเหลว | คงค่าฟอร์มและแสดงวิธีแก้หรือปุ่มลองอีกครั้ง |

ใช้ toast สำหรับ feedback สั้น เช่น ส่งออกสำเร็จ แต่ข้อผิดพลาดที่ขัดขวางงานต้องมีข้อความบนหน้า/ฟอร์มด้วย ใช้ `role="status"` สำหรับผลลัพธ์ที่เปลี่ยนและ `role="alert"` สำหรับข้อผิดพลาดอย่างพอดี

งาน polling ต้อง cleanup เมื่อออกจากหน้า ป้องกันคำขอซ้อน คำขอค้าง และ response ที่หมดความเกี่ยวข้อง ห้ามถือว่า API ล้มเหลวหมายถึงอุปกรณ์ออนไลน์หรือมีจำนวนเป็นศูนย์

## 10. ฟอร์มและ modal

- ทุกช่องมี label ที่เชื่อมกับ control; placeholder เป็นตัวอย่าง ไม่ใช้แทน label
- แสดงช่องบังคับและข้อจำกัด เช่น รูปแบบไฟล์หรือขนาด ก่อนผู้ใช้ส่งข้อมูล
- วางข้อผิดพลาดใกล้ช่อง เชื่อมด้วย `aria-describedby` และ `aria-invalid` ตามสถานะ
- ปุ่ม “ยกเลิก” อยู่ก่อน “บันทึก” ในลำดับการอ่าน ใช้ตำแหน่งเดียวกันในฟอร์มประเภทเดียวกัน
- อย่าล้างฟอร์มเมื่อ API ล้มเหลว และเตือนก่อนทิ้งการแก้ไขที่ยังไม่บันทึกเมื่อมีโอกาสเสียงาน
- Modal มีชื่อ dialog, `aria-modal`, initial focus, focus trap และคืน focus ไปยังตัวเปิดเมื่อปิด
- รองรับ Escape และปุ่มปิดที่มีชื่อ; การคลิก backdrop ต้องไม่ทำให้ค่าที่กรอกหายโดยไม่มีการจัดการ
- ฟอร์มยาวใช้หน้าเต็มได้ ไม่ฝืนย่อทุก flow ลงใน modal; modal ต้องเลื่อนเนื้อหาได้บนจอเล็ก

## 11. Responsive และ accessibility

- ใช้ layout ที่ wrap ได้ และ `min-width: 0` กับ flex/grid children ที่มีข้อความยาว
- บนมือถือให้ช่องค้นหาเต็มความกว้าง และเรียง controls ตามลำดับงาน
- Sidebar ปัจจุบันเปลี่ยนรูปแบบที่ 1024px; Devices ปรับ toolbar ที่ 600px ใช้ breakpoint ตามเนื้อหา ไม่เพิ่มค่าที่ต่างกันโดยไม่มีเหตุผล
- ตารางกว้างเลื่อนเฉพาะบริเวณตารางได้ ห้ามทำให้ทั้งหน้าเลื่อนแนวนอนหรือซ่อนคอลัมน์สำคัญโดยไม่มีทางดู
- Empty/error state ต้องอ่านได้ใน viewport แม้ตารางจะกว้างกว่าจอ
- แสดง focus ชัดเจน ไม่ลบ outline โดยไม่มีตัวแทน ใช้ฐาน 3px และ offset 3px ตาม Devices
- หลีกเลี่ยง `div onClick` สำหรับ controls; ผู้ใช้ต้องทำงานหลักได้ด้วย Tab, Enter และ Space ตาม semantic element
- เคารพ `prefers-reduced-motion` ทั้ง animation ภายในหน้าและ page transition จาก parent
- ตรวจข้อความไทยยาว ชื่อสำนักงานยาว การซูม 200% และสีทั้ง light/dark

## 12. โครงสร้างโค้ดสำหรับขยายต่อ

- แยก style คงที่ไว้ใน CSS ที่ scope ด้วยชื่อหน้า ใช้ inline style เฉพาะค่าที่ขึ้นกับข้อมูล
- ก่อนแก้ global selector เช่น `.card`, `.glass`, `button` ให้ตรวจทุกหน้าที่ได้รับผล
- เมื่อนำรูปแบบไปใช้หน้าที่สอง ให้พิจารณาสกัดคอมโพเนนต์ร่วม เช่น `Button`, `PageHeader`, `FormField`, `StatusBadge`, `TableToolbar`, `Pagination`, `DataState` และ `Modal`
- ชื่อข้างต้นเป็นเป้าหมายการจัดโครงสร้าง ไม่ใช่ API ที่มีอยู่แล้ว อย่า import จนกว่าจะสร้างจริง
- คอมโพเนนต์กลางควรรับหน้าตาและพฤติกรรมพื้นฐาน ส่วน API, business rules และสิทธิ์อยู่ที่ feature
- ไม่คัดลอก CSS ทั้งหน้า Devices ไปเปลี่ยน prefix ในทุกหน้า ให้ย้ายส่วนร่วมเมื่อมีการใช้งานซ้ำจริง
- หากเปลี่ยน feature ที่ผู้ใช้เห็น ให้อัปเดตทั้ง `GUIDE_SECTIONS` ใน [About.jsx](src/components/About.jsx) และ [USER_GUIDE.md](USER_GUIDE.md) ให้ตรงกัน

## 13. Checklist ก่อนส่งงาน

- [ ] หัวหน้า toolbar ปุ่ม และ pagination ตรงกับรูปแบบหน้าประเภทเดียวกัน
- [ ] ภาษา หน่วย สีสถานะ และขอบเขตการส่งออกชัดเจน
- [ ] ตรวจ loading, success, empty, no results, error และ stale data
- [ ] ค้นหา → เปิดรายละเอียด → กลับ ด้วยทั้งปุ่มในแอปและ browser Back แล้วบริบทถูกต้อง
- [ ] ตรวจ refresh, จำนวนข้อมูลลดลง, storage ใช้ไม่ได้ และค่าที่เก็บไม่ถูกต้อง หากมี persistence
- [ ] ตรวจ keyboard, focus, accessible names และ modal ตามที่มีในงาน
- [ ] ตรวจ light/dark ที่ความกว้าง 360, 768 และ 1280px และข้อความไทยยาว
- [ ] ตรวจสิทธิ์ผู้ใช้และไฟล์ส่งออกโดยไม่เปลี่ยน business rules เดิม
- [ ] รัน lint ที่เกี่ยวข้องและ production build; ทดสอบพฤติกรรมที่เสี่ยงเปลี่ยน ไม่เพิ่ม test ที่ตรวจเพียงค่ารูปแบบคงที่
- [ ] อัปเดตคู่มือหาก flow เปลี่ยน และระบุส่วนที่ยังไม่ได้ทดสอบในผลส่งงาน

เมื่อจำเป็นต้องยกเว้นมาตรฐาน ให้บันทึกเหตุผล ผู้ใช้ที่ได้รับผล และวิธีทดสอบในรายละเอียดงาน เพื่อให้ผู้พัฒนาคนถัดไปเข้าใจการตัดสินใจ
