# แผนปรับปรุงหน้าหลัก NE2 LDAP

วันที่: 24 กันยายน 2026  
หน้า: `/` — แผนที่สำนักงาน กฟฉ.2  
สถานะ: แผนสำหรับพัฒนา ยังไม่ได้ปรับโค้ดหน้าหลัก

ตรวจจาก `src/components/SitesMap.jsx`, การนำทางใน `src/App.jsx` และมาตรฐาน `DESIGN_STANDARDS.md` ข้อค้นพบนี้มาจากโค้ด ยังไม่ได้ยืนยันการแสดงผลหรือ schema API ด้วยเบราว์เซอร์ในรอบนี้

## 1. เป้าหมายและ feature ที่ต้องรักษา

หน้าหลักควรตอบได้เร็วว่า “สำนักงานใดมีปัญหา อยู่ที่ไหน และเปิดดูรายละเอียดได้อย่างไร” โดยยังคงแผนที่เป็นองค์ประกอบหลัก

- คงตัวกรองจังหวัด ประเภทสำนักงาน และสถานะ
- คงหมุดสำนักงาน การเลื่อน/ซูม popup และลิงก์รายละเอียดอุปกรณ์
- คงโหมดเต็มจอและการอัปเดตสถานะทุก 60 วินาที
- เพิ่มช่องค้นหาชื่อสำนักงาน รายการสำนักงานที่ใช้แทนแผนที่ได้ และข้อมูลความสดของสถานะ
- รองรับ desktop/mobile, light/dark และผู้ใช้สูงวัย: ข้อความอ่านง่าย ปุ่มชัด สถานะมีข้อความและสัญลักษณ์ประกอบสี
- ไม่เพิ่มฟอร์มแก้ไขข้อมูลหรือเปลี่ยนสิทธิ์ใช้งานในงานนี้

## 2. ปัญหาที่พบและความสำคัญ

| ระดับ | จุดในโค้ดปัจจุบัน | แนวทางแก้ |
|---|---|---|
| P1 | `statusOf` ให้ทุกค่าที่ไม่ใช่ up เป็น down รวม metrics ที่ไม่มี | แยก up/down/unknown; ข้อมูลหายไม่เท่ากับอุปกรณ์ขัดข้อง |
| P1 | `sitesWithCoords` ตัดสำนักงานที่ไม่มี network_device ออกด้วย | นับสำนักงานทั้งหมดก่อน แยกไม่มีอุปกรณ์และไม่มีพิกัดให้เห็นในรายการ |
| P1 | ตรวจพิกัดแค่ไม่เป็น null แล้วใช้ Number | ตรวจ finite, ค่าว่าง และช่วง latitude/longitude ป้องกันค่าผิดทำแผนที่ล้ม |
| P1 | ไม่ตรวจ HTTP status/shape อย่างครบถ้วน | ห้ามแปลง response ผิดรูปแบบเป็นรายการว่าง ต้องแสดง error พร้อม retry |
| P1 | Background refresh ล้มเหลวไม่แจ้งผู้ใช้ | คงข้อมูลเดิมพร้อมป้ายข้อมูลเก่าและเวลาโหลดสำเร็จล่าสุด |
| P1 | Polling ไม่มี abort/timeout/ป้องกันคำขอซ้อน | เพิ่ม lifecycle ที่ยกเลิกได้และไม่ให้ response เก่าทับข้อมูลใหม่ |
| P2 | ตัวกรองอยู่ใน state และ map ถูก unmount เมื่อผลเป็นศูนย์ | รักษาบริบทตัวกรอง center/zoom และสำนักงานที่เลือกเมื่อย้อนกลับหรือ clear filter |
| P2 | ไม่มีค้นหาชื่อ และมีเพียงแผนที่ให้เลือกสำนักงาน | เพิ่มรายการคู่แผนที่และค้นหา เพื่อใช้ได้เมื่อหมุดทับกันหรืออ่านแผนที่ไม่สะดวก |
| P2 | แผนที่สูงคงที่ 600px | ปรับตาม viewport โดยสงวนพื้นที่ตัวกรองและไม่บังคับเลื่อนมากบนมือถือ |
| P2 | หมุด 16px และแยกสถานะด้วยสีแดง/เขียว | เพิ่มพื้นที่แตะ พร้อมสัญลักษณ์/legend/ชื่อที่อ่านด้วย assistive technology ได้ |
| P2 | Fullscreen ไม่จัดการ promise rejection และตรวจ document.fullscreenElement แบบรวม | ตรวจว่า element ของแผนที่เต็มจอจริง รองรับ API ใช้ไม่ได้ และคืน focus |
| P2 | MapResizer ตรวจเฉพาะ fullscreen | รองรับ sidebar ยุบ/เปิด และการเปลี่ยนขนาด panel ด้วย ResizeObserver |

## 3. โครงหน้าเสนอ

```text
แผนที่สำนักงาน กฟฉ.2                           [รีเฟรช]
สถานะการเชื่อมต่อเครือข่าย · โหลดล่าสุด … · อัปเดตทุก 1 นาที

[สำนักงานทั้งหมด] [ออนไลน์] [ขัดข้อง] [ไม่ทราบสถานะ] [ยังไม่มีอุปกรณ์]

ค้นหาสำนักงาน | จังหวัด | ประเภทสำนักงาน | สถานะ | ล้างตัวกรอง
แสดง N จาก M สำนักงาน · มีพิกัด X · ไม่มีพิกัด Y

Desktop:
┌──────────────────────────────┬──────────────────────┐
│ แผนที่                       │ รายการสำนักงาน       │
│                              │ ชื่อ / จังหวัด / สถานะ│
│ [ซูม] [ดูหมุดทั้งหมด] [เต็มจอ]│ [ดูบนแผนที่] [รายละเอียด]│
└──────────────────────────────┴──────────────────────┘

Mobile:
[แผนที่] [รายการสำนักงาน]
แสดงเนื้อหาตามมุมมองที่เลือก โดยใช้ตัวกรองชุดเดียวกัน
```

- Summary นับสำนักงาน ไม่ใช้คำว่าอุปกรณ์ถ้าจำนวนไม่ได้มาจากอุปกรณ์จริง
- Summary แสดงยอดทั้งชุดที่โหลดและระบุขอบเขตชัดเจน; จำนวนผลลัพธ์ใต้ตัวกรองเป็นยอดหลังกรอง
- การกด summary สถานะตั้งเฉพาะตัวกรองสถานะ โดยคงจังหวัด ประเภท และคำค้น ผู้ใช้จึงไล่ดูสถานะภายในขอบเขตเดิมได้
- สีไม่ใช่ตัวสื่อสารเพียงอย่างเดียว: ออนไลน์ ✓, ขัดข้อง !, ไม่ทราบสถานะ ?, ไม่มีอุปกรณ์ —

## 4. ตัวกรองและบริบท

- ใช้ shared ListPage controls: label ชัดเจน ความสูงอย่างน้อย 44px radius 8px ขนาดอักษร 16px สำหรับช่องกรอก
- ช่องค้นหารองรับชื่อสำนักงาน จังหวัด และประเภทจาก field ที่มีจริง ไม่โฆษณาว่าค้น IP ได้จนตรวจ schema และสิทธิ์แล้ว
- จังหวัด/ประเภทใช้ searchable dropdown เมื่อรายการยาว; สถานะใช้ fixed enum
- ไฮไลท์ทันทีเมื่อมีค่า รวมค่า restore; “ทั้งหมด” และช่องว่างไม่ไฮไลท์
- กรองฝั่ง client จากรายการที่โหลดครบแล้ว; debounce ช่องข้อความประมาณ 300ms โดยไม่ยิง API ทุกตัวอักษร
- เก็บ query, province, type, status และมุมมอง map/list ใน URL เพื่อรองรับ refresh และ Back/Forward
- เก็บ center, zoom, selected site, scroll และ focus ใน sessionStorage ตามบริบทหน้า พร้อม try/catch และ validate ค่า
- เพิ่มปุ่ม “ดูหมุดทั้งหมด” เพื่อ fitBounds ของผลที่กรอง ห้าม polling บังคับเลื่อนหรือซูมแผนที่เอง
- เมื่อล้างตัวกรองให้กลับชุดข้อมูลเต็ม แต่ไม่เปลี่ยนปี/เงื่อนไขของหน้าอื่น ไม่ใช้การล้าง sessionStorage ทั้งระบบ
- ตัวเลือกที่เก็บไว้แต่ไม่มีในข้อมูลใหม่ต้องยังแสดงค่าเดิมพร้อมข้อความ ไม่ลบเงื่อนไขอย่างเงียบ ๆ

## 5. แผนที่และรายการสำนักงาน

### แผนที่

- ตอนเปิดครั้งแรกที่ยังไม่มี viewport เก่า ให้ fitBounds พิกัดที่ผ่าน validation; ถ้าไม่มีพิกัดใช้ DEFAULT_CENTER เดิมและข้อความอธิบาย
- คง MapContainer ไว้เมื่อกรองไม่พบข้อมูล ใช้ empty overlay เพื่อไม่ทำ center/zoom หาย
- หมุดมีพื้นที่กดอย่างน้อย 44px โดยตัวสัญลักษณ์ภายในเล็กกว่าได้ ระวังพื้นที่ hit ทับกัน
- เมื่อตำแหน่งเดียวกันมีหลายสำนักงาน ต้องมีทางเลือกผ่านรายการ หากข้อมูลจริงหนาแน่นจึงพิจารณา clustering พร้อมชื่อ/จำนวนและการแตกกลุ่ม ไม่เพิ่ม dependency จนประเมินความจำเป็น
- กดรายการสำนักงานแล้วเลือกหมุดและเปิด popup; ปุ่ม “ดูบนแผนที่” บน mobile สลับมุมมองก่อนแสดงสำนักงานนั้น
- ไม่ทำ auto-fit ทุกครั้งที่ refresh เพราะจะขัดการตรวจพื้นที่ของผู้ใช้
- แยก tile layer ล้มเหลวจาก API สถานะล้มเหลว: ถ้าแผนที่พื้นหลังโหลดไม่ได้ ให้ยังใช้รายการและข้อมูลสำนักงานได้
- คง OpenStreetMap attribution ให้มองเห็นได้ รวมเต็มจอ ห้ามปรับสี tile ด้วย filter จนชื่อสถานที่อ่านยากใน dark mode

### Popup

- แสดงชื่อสำนักงานเต็ม จังหวัด ประเภท ชื่ออุปกรณ์ สถานะ และ latency เมื่อมีค่าจริง
- แสดงเวลาตรวจวัดเมื่อ API ส่งมา; หากไม่มี ไม่ใช้เวลาโหลดหน้าแทนเวลาตรวจวัด
- ปุ่ม “ดูรายละเอียดอุปกรณ์” เป็น anchor href จริง รองรับเปิดแท็บใหม่ และใช้ navigation ของแอปสำหรับคลิกปกติ
- ไม่มีอุปกรณ์ให้แสดง “ยังไม่มีอุปกรณ์เครือข่าย” และไม่สร้างลิงก์ที่ไม่มี id
- ใช้ padding 16px, ฟอนต์ 14–16px, สีจาก theme และ max-width ที่ไม่ล้นจอ 360px

### รายการสำนักงาน

- แสดงทุกสำนักงานที่ตรง filter รวมไม่มีพิกัดและไม่มีอุปกรณ์ ไม่ให้การขาดพิกัดทำให้สำนักงานหายจากหน้าหลัก
- แต่ละรายการมีชื่อ จังหวัด/ประเภท สถานะข้อความ และปุ่มที่แยก “ดูบนแผนที่” กับ “ดูรายละเอียดอุปกรณ์” ชัดเจน
- ไม่มีพิกัด: แสดงข้อความและปิดเฉพาะปุ่มดูบนแผนที่; รายละเอียดอุปกรณ์ยังเปิดได้ถ้ามี id
- มี pagination หรือรายการที่โหลดเพิ่มเพื่อไม่สร้าง DOM จำนวนมาก; การค้นหาและยอดรวมต้องคำนวณจากข้อมูลครบ ไม่ใช่เฉพาะหน้าปัจจุบัน
- ถ้าพบข้อมูลถูกกรองออกหรือสำนักงานที่เลือกถูกลบ ให้แจ้งและเคลียร์ selection อย่างปลอดภัย ไม่เปิด popup ค้างของ record เก่า

## 6. สถานะและความถูกต้องของตัวเลข

| สถานการณ์ | สถานะที่แสดง |
|---|---|
| มีอุปกรณ์และ metrics.status === up | ออนไลน์ |
| มีอุปกรณ์และ metrics.status === down | ขัดข้อง |
| มีอุปกรณ์แต่ metrics ไม่มี/สถานะไม่รู้จัก | ไม่ทราบสถานะ |
| ไม่มีอุปกรณ์ | ยังไม่มีอุปกรณ์เครือข่าย |
| พิกัดว่าง/ผิดรูปแบบ | ไม่มีพิกัดที่ใช้แสดงได้ เป็นคุณสมบัติแยกจากสถานะเครือข่าย |

- ยอดทั้งหมด = ออนไลน์ + ขัดข้อง + ไม่ทราบสถานะ + ไม่มีอุปกรณ์ ภายใต้ขอบเขตการนับเดียวกัน
- จำนวนไม่มีพิกัดซ้อนกับทุกสถานะได้ ห้ามบวกเพิ่มเป็นสถานะที่ห้า
- พิกัดยอมรับ numeric string ที่แปลงได้ แต่ไม่ยอมรับสตริงว่าง/ช่องว่าง, NaN, Infinity หรือเกินช่วง ±90/±180; พิกัด 0 เป็นค่าที่ถูกต้องทางภูมิศาสตร์ ไม่ตัดโดยใช้ truthiness
- latency null แสดง “—”; 0 แสดง 0 ms; ไม่เอาค่าตรวจครั้งเก่ามาอ้างว่าเป็นค่าปัจจุบันโดยไม่มีเวลา
- ถ้าต้องมีสถานะ stale ตามอายุ metrics ต้องตกลงเกณฑ์ร่วมกับหน้าเครือข่ายก่อน ไม่สมมุติอายุข้อมูลเอง

## 7. การโหลดข้อมูลและ API

ใช้ `GET /api/pea-sites` เดิมก่อน โดยตรวจ response จริงและยืนยันว่าครบทุกหน้าหรือมี pagination ก่อนคำนวณยอด

ข้อมูลขั้นต่ำที่ต้องมี:

| กลุ่ม | Fields |
|---|---|
| สำนักงาน | id, pea_name, pea_province, pea_type, latitude, longitude |
| อุปกรณ์ | network_device.id, network_device.pea_name หรือ null |
| สถานะ | network_device.metrics.status, latency_ms หรือ null |
| ข้อมูลเพิ่มเติมที่ควรยืนยัน | ชื่อ field เวลาตรวจวัดจริง, source_updated_at, pagination และกรณีสำนักงานมีอุปกรณ์หลายตัว |

- หากข้อมูลเป็นหนึ่งอุปกรณ์ต่อสำนักงานตาม response ปัจจุบัน ให้อธิบายว่าเป็นสถานะอุปกรณ์ที่ผูกอยู่ ไม่อ้างว่าเป็นสุขภาพเครือข่ายทั้งหมดของสำนักงาน
- ไม่จำเป็นต้องเพิ่ม endpoint ใหม่สำหรับ layout, ตัวกรอง หรือรายการคู่แผนที่ หาก API เดิมคืนข้อมูลครบและปริมาณเหมาะสม
- แยก loading ครั้งแรก, refreshing, error, empty, no-results และ stale
- ตรวจ response.ok, success และ array shape; request ล้มเหลวต้องคงข้อมูลสำเร็จล่าสุดพร้อมแถบแจ้งเตือน
- มีปุ่มรีเฟรช/ลองใหม่, AbortController, timeout, request guard, cleanup ตอนออกหน้า และไม่ให้ polling ซ้อน
- พัก polling เมื่อแท็บซ่อน และตรวจอายุข้อมูลก่อนรีเฟรชเมื่อกลับมา
- แสดง “โหลดสำเร็จล่าสุด” จากเวลา client และ “ตรวจวัดล่าสุด” จาก backend แยกกัน

## 8. Desktop/mobile และ accessibility

- Desktop: map/list สัดส่วนประมาณ 2:1 โดย sidebar รายการกว้างอย่างน้อย 300px; ถ้าพื้นที่เนื้อหาไม่พอให้ใช้มุมมองสลับเหมือน tablet ไม่ฝืนบีบ map
- Tablet/mobile: toggle “แผนที่ / รายการ” ใช้ปุ่มที่มีสถานะ aria-pressed; ไม่มีการโหลด filter ใหม่เมื่อสลับ
- ความสูง map ใช้ clamp ตาม viewport เช่น 360–680px บน desktop; mobile ประมาณ 55dvh ที่มี min/max เหมาะสม และโหมดเต็มจอใช้ 100dvh
- ตัวกรองมือถือเต็มแถว ปุ่มล้างแยกชัด; การ์ดสรุป 1–2 คอลัมน์ตามพื้นที่ ข้อความจำนวนมากไม่ทำให้หน้าล้น
- Panel radius 16px, control 8px, ไม่ใช้ card hover ยกแผนที่หรือทำให้ popup ขยับ
- Zoom controls, fullscreen และ action สำคัญมี target 44px; ชื่อปุ่มชัดและมี focus-visible
- ใช้ label ผูก control, summary/ผลลัพธ์ประกาศด้วย role=status อย่างพอดี ไม่ประกาศสถานะทั้งแผนที่ใหม่ทุกนาที
- คีย์บอร์ดเลือกสำนักงานและเปิดรายละเอียดได้ผ่านรายการ ไม่ต้องบังคับเดินทุกหมุด
- เคารพ reduced motion ทั้ง page transition และการ pan/zoom ที่สั่งจากรายการ
- Fullscreen ต้องรองรับ rejection/unsupported พร้อม fallback ขยาย panel ภายในหน้า; ใช้ mapCardRef เทียบ fullscreenElement, exit เมื่อไปหน้ารายละเอียด และคืน focus ปุ่มเปิดเมื่อปิด
- ใช้ ResizeObserver กับพื้นที่ map เพื่อ invalidateSize เมื่อ sidebar, รายการ หรือ viewport เปลี่ยน

## 9. โครงสร้างโค้ดและขั้นตอนพัฒนา

1. ตรวจ schema และข้อมูลตัวอย่างจริง: status หาย, ไม่มีอุปกรณ์, ไม่มีพิกัด, พิกัดซ้ำ และจำนวนสำนักงานทั้งหมด
2. แยก normalize/status/count helpers และ hook โหลดข้อมูลพร้อม test ที่ตรวจกรณีผิดพลาดจริงก่อนเปลี่ยน layout
3. สร้าง `SitesMap.css` ที่ scope หน้า ใช้ ListPage controls และไฮไลท์ filter ร่วม
4. เพิ่ม query/session persistence และการกลับจาก DeviceDetails สู่แผนที่เดิม ปัจจุบัน App ส่ง onBack ของ DeviceDetails ไปหน้า devices เสมอ ต้องเพิ่ม return context สำหรับต้นทางแผนที่พร้อม fallback กรณีลิงก์ตรง
5. เพิ่ม summary และรายการสำนักงาน รวมการเลือกหมุดจากรายการและ popup ที่อ่านง่าย
6. ปรับ responsive/fullscreen/tile error และทดสอบ navigation กับ polling ร่วมกัน
7. อัปเดต GUIDE_SECTIONS ใน About.jsx, USER_GUIDE.md และ DESIGN_STANDARDS.md เฉพาะกติกาที่ใช้ร่วมได้

ไฟล์เป้าหมาย: `SitesMap.jsx`, `SitesMap.css` (ใหม่), hook/helper ของแผนที่ (ใหม่), `App.jsx` และคู่มือ หลีกเลี่ยงแก้ global .glass/.card เพื่อไม่กระทบหน้าอื่น

## 10. Checklist ตรวจรับ

- [ ] Feature เดิม: จังหวัด/ประเภท/สถานะ, popup, รายละเอียดอุปกรณ์, ซูม/เลื่อน, เต็มจอ และ polling ยังทำงาน
- [ ] metrics หายไม่ถูกนับเป็น down และไม่มีอุปกรณ์ไม่ถูกซ่อนจากรายการ
- [ ] จำนวนสถานะรวมเท่ากับจำนวนสำนักงาน; จำนวนมีพิกัดกับไม่มีพิกัดตรงชุดข้อมูลเดียวกัน
- [ ] พิกัด null/ว่าง/สตริงผิด/เกินช่วง/0 ไม่ทำให้แผนที่ crash
- [ ] ค้นหาและกรอง → เลือกสำนักงาน → รายละเอียด → กลับ ด้วยปุ่มแอปและ browser คืนตัวกรอง zoom/center และบริบทเดิม
- [ ] กรองไม่พบ → ล้าง → ข้อมูลกลับมาโดยไม่รีเซ็ต map โดยไม่จำเป็น
- [ ] เปลี่ยน filter/ออกหน้า/เปลี่ยนแท็บระหว่างโหลด ไม่มี response เก่าหรือ polling ค้าง
- [ ] API error, timeout, malformed response และ tile error แสดงต่างจากไม่มีข้อมูล พร้อม retry
- [ ] Fullscreen เปิด/ปิด/Escape/unsupported และนำทางจาก popup ขณะเต็มจอไม่ทำหน้าค้าง
- [ ] ตรวจ 360/390/768/1280/1440px, sidebar เปิด/ปิด, light/dark, ชื่อไทยยาว, zoom 200% และ reduced motion
- [ ] คีย์บอร์ดใช้งานผ่านรายการและ controls ได้ครบ ไม่มี focus ถูกแย่งตอน refresh
- [ ] ตรวจ lint, build และ browser interaction ด้วย mock/read-only API โดยไม่แก้ข้อมูลสำนักงานจริง

## 11. ข้อมูลที่ต้องยืนยันในขั้นพัฒนา

ชื่อ field เวลาตรวจวัดและรูปแบบ pagination ของ `/api/pea-sites`, ความสัมพันธ์สำนักงานกับจำนวนอุปกรณ์ และเกณฑ์ข้อมูลเก่าร่วมกับหน้าเครือข่าย หาก backend ยังไม่ส่งเวลา ให้แสดงเฉพาะเวลาโหลดและระบุว่าไม่ทราบเวลาตรวจวัด โดยยังพัฒนาส่วน layout/navigation ต่อได้
